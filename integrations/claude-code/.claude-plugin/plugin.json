{
  "name": "mubit-memory",
  "displayName": "Mubit Memory",
  "version": "0.10.0",
  "description": "Persistent, typed, self-improving memory for Claude Code, backed by Mubit. Captures work involuntarily, recalls relevant lessons before every prompt, and attributes outcomes so memory improves with use.",
  "author": { "name": "Mubit AI", "url": "https://mubit.ai" },
  "homepage": "https://docs.mubit.ai/integrations/claude-code",
  "repository": "https://github.com/mubit-ai/claude-plugins",
  "license": "Apache-2.0",
  "keywords": ["memory", "mubit", "long-term-memory", "lessons", "recall", "rag"],
  "userConfig": {
    "endpoint": { "type": "string", "title": "Mubit endpoint", "default": "",
      "description": "Your Mubit instance URL, e.g. https://eu.mubit.ai. Required." },
    "apiKey": { "type": "string", "title": "API key", "default": "", "sensitive": true,
      "description": "Mubit API key (mbt_...). Stored in the OS keychain, never in a settings file." },
    "userId": { "type": "string", "title": "User id", "default": "",
      "description": "Optional user/entity id for multi-user memory scoping." },
    "runStrategy": { "type": "string", "title": "Run strategy", "default": "per-directory",
      "description": "How a Claude Code session maps to a Mubit run: per-directory, git-branch, per-conversation, or static." },
    "capture": { "type": "boolean", "title": "Capture tool activity", "default": true,
      "description": "Capture tool activity into Mubit memory." },
    "recall": { "type": "boolean", "title": "Inject recalled memory", "default": true,
      "description": "Inject recalled memory before each prompt." },
    "redact": { "type": "boolean", "title": "Redact before sending", "default": true,
      "description": "Scrub secrets and denylisted paths before anything leaves the machine. Turning this off is not recommended." },
    "recallTokenBudget": { "type": "number", "title": "Recall token budget", "default": 1500,
      "description": "Maximum tokens of recalled context injected per prompt." },
    "recallMaxPerSection": { "type": "number", "title": "Recall items per section", "default": 0,
      "description": "Maximum items rendered per section of the injected block. 0 means no cap, and the token budget is the only limit." },
    "recallAssemble": { "type": "string", "title": "Recall assembly", "default": "client",
      "description": "How recalled memory is assembled: 'client' is free; 'server' uses /v2/control/context, which costs two LLM calls per prompt." },
    "reflectOnEnd": { "type": "boolean", "title": "Reflect on session end", "default": true,
      "description": "Reflect when a session ends. This is the only path that promotes a lesson beyond its own run — turning it off costs cross-session memory." },
    "outcomeMode": { "type": "string", "title": "Outcome attribution", "default": "implicit",
      "description": "How turn outcomes are attributed back to recalled memories: off, implicit, or explicit." },
    "statusLine": { "type": "boolean", "title": "Status line", "default": true,
      "description": "Show Mubit connection and capture stats in the status line." },
    "mcpTools": { "type": "string", "title": "MCP tool allowlist", "default": "",
      "description": "Comma-separated allowlist of Mubit MCP tools. Blank uses the curated default set." },
    "mcpLessonScope": { "type": "string", "title": "MCP lesson scope ceiling", "default": "run",
      "description": "The widest scope a lesson written by an MCP tool may claim: run, session, or global. Anything wider than run is read by unrelated runs, so the default keeps an agent's lesson in the run that wrote it; reflection is still the path that promotes one beyond it." }
  }
}
