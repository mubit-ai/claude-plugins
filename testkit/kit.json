{
  "kitVersion": "1.0.0",
  "model": "sonnet",
  "reps": 3,
  "canary": "what conventions and constraints apply to this project",
  "recallBudgetMs": 1500,
  "maxCostUsd": 5,
  "_notes": {
    "model": "An alias, not an id, so that a sweep run today and one run in six months use the model each was meant to. The resolved id is captured per trial from system/init and `compare` warns when two runs resolved differently — which is the only place the distinction can actually mislead.",
    "recallBudgetMs": "MUBIT_CC_RECALL_BUDGET_MS, internal to the recall hook. Not the 3s UserPromptSubmit registration timeout the host enforces.",
    "reps": "3 gives 2 scored reps per pair after the warm-up rep is dropped. Six non-tied pairs is the minimum at which a two-sided sign test can reach p<0.05 at all, so 3 reps x 3+ cases is the smallest sweep that can produce a significant result."
  }
}
